apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: usage-service
  description: usage microservice.
  annotations:
    github.com/project-slug: huzaifa678/usage-service
    # Argo CD app names are <service>-<env>; dev shown by default.
    argocd/app-name: usage-service-dev
    # Pods carry label app=<service>, in namespace saas-apps (Argo Rollouts).
    backstage.io/kubernetes-label-selector: app=usage-service
    backstage.io/kubernetes-namespace: saas-apps
spec:
  type: service
  lifecycle: production
  owner: platform-team
  system: saas-services
