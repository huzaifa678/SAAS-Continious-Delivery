apiVersion: v1
kind: Service
metadata:
  name: keycloak-postgres
  namespace: keycloak
spec:
  selector:
    app: postgres
    role: master
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP