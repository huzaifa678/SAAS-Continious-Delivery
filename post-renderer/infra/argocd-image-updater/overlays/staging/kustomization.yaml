---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

commonLabels:
  environment: staging

patches:
  - target: { kind: Deployment, name: argocd-image-updater }
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: argocd-image-updater
      spec:
        template:
          spec:
            containers:
              - name: argocd-image-updater
                resources:
                  requests: { cpu: 25m, memory: 32Mi }
                  limits: { cpu: 100m, memory: 64Mi }
