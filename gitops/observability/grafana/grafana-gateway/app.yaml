# Git-files generator config for the `observability` ApplicationSet.
# Discovered via gitops/observability/<stack>/*/app.yaml; keys become params.
# Syncs after the grafana stack is up.
syncWave: "1"
