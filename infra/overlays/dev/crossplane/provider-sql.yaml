# ─────────────────────────────────────────────────────────────────────────────
# GENERATED by scripts/render_gitops.py from the Terraform gitops_contract,
# using the templates under scripts/templates/*.tpl.
# Do not edit by hand — change the contract in saas-services-infra (or the
# template) and re-render.
# ─────────────────────────────────────────────────────────────────────────────
---
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-sql
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  package: xpkg.upbound.io/crossplane-contrib/provider-sql:v0.10.0
  packagePullPolicy: IfNotPresent
  revisionActivationPolicy: Automatic
  revisionHistoryLimit: 1

# ─── airflow ───────────────────────────────────────────────────────────────
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: rds-airflow-admin
  namespace: crossplane-system
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: rds-airflow-admin
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        data: |
          postgresql://{{ .username }}:{{ .password }}@{{ .endpoint }}/{{ .db_name }}?sslmode=require
        username: "{{ .username }}"
        password: "{{ .password }}"
        endpoint: "{{ .endpoint }}"
        db_name: "{{ .db_name }}"
  data:
    - { secretKey: username, remoteRef: { key: saas-airflow-db-secret, property: username } }
    - { secretKey: password, remoteRef: { key: saas-airflow-db-secret, property: password } }
    - { secretKey: endpoint, remoteRef: { key: saas-airflow-db-secret, property: endpoint } }
    - { secretKey: db_name,  remoteRef: { key: saas-airflow-db-secret, property: db_name  } }
---
apiVersion: postgresql.sql.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: rds-airflow
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  credentials:
    source: PostgreSQLConnectionSecret
    connectionSecretRef:
      namespace: crossplane-system
      name: rds-airflow-admin
  sslMode: require

# ─── billing ───────────────────────────────────────────────────────────────
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: rds-billing-admin
  namespace: crossplane-system
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: rds-billing-admin
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        data: |
          postgresql://{{ .username }}:{{ .password }}@{{ .endpoint }}/{{ .db_name }}?sslmode=require
        username: "{{ .username }}"
        password: "{{ .password }}"
        endpoint: "{{ .endpoint }}"
        db_name: "{{ .db_name }}"
  data:
    - { secretKey: username, remoteRef: { key: saas-billing-db-secret, property: username } }
    - { secretKey: password, remoteRef: { key: saas-billing-db-secret, property: password } }
    - { secretKey: endpoint, remoteRef: { key: saas-billing-db-secret, property: endpoint } }
    - { secretKey: db_name,  remoteRef: { key: saas-billing-db-secret, property: db_name  } }
---
apiVersion: postgresql.sql.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: rds-billing
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  credentials:
    source: PostgreSQLConnectionSecret
    connectionSecretRef:
      namespace: crossplane-system
      name: rds-billing-admin
  sslMode: require

# ─── keycloak ───────────────────────────────────────────────────────────────
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: rds-keycloak-admin
  namespace: crossplane-system
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: rds-keycloak-admin
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        data: |
          postgresql://{{ .username }}:{{ .password }}@{{ .endpoint }}/{{ .db_name }}?sslmode=require
        username: "{{ .username }}"
        password: "{{ .password }}"
        endpoint: "{{ .endpoint }}"
        db_name: "{{ .db_name }}"
  data:
    - { secretKey: username, remoteRef: { key: keycloak-db-secret, property: username } }
    - { secretKey: password, remoteRef: { key: keycloak-db-secret, property: password } }
    - { secretKey: endpoint, remoteRef: { key: keycloak-db-secret, property: endpoint } }
    - { secretKey: db_name,  remoteRef: { key: keycloak-db-secret, property: db_name  } }
---
apiVersion: postgresql.sql.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: rds-keycloak
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  credentials:
    source: PostgreSQLConnectionSecret
    connectionSecretRef:
      namespace: crossplane-system
      name: rds-keycloak-admin
  sslMode: require

# ─── subscription ───────────────────────────────────────────────────────────────
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: rds-subscription-admin
  namespace: crossplane-system
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: rds-subscription-admin
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        data: |
          postgresql://{{ .username }}:{{ .password }}@{{ .endpoint }}/{{ .db_name }}?sslmode=require
        username: "{{ .username }}"
        password: "{{ .password }}"
        endpoint: "{{ .endpoint }}"
        db_name: "{{ .db_name }}"
  data:
    - { secretKey: username, remoteRef: { key: saas-subscription-db-secret, property: username } }
    - { secretKey: password, remoteRef: { key: saas-subscription-db-secret, property: password } }
    - { secretKey: endpoint, remoteRef: { key: saas-subscription-db-secret, property: endpoint } }
    - { secretKey: db_name,  remoteRef: { key: saas-subscription-db-secret, property: db_name  } }
---
apiVersion: postgresql.sql.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: rds-subscription
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  credentials:
    source: PostgreSQLConnectionSecret
    connectionSecretRef:
      namespace: crossplane-system
      name: rds-subscription-admin
  sslMode: require

# ─── usage ───────────────────────────────────────────────────────────────
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: rds-usage-admin
  namespace: crossplane-system
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: rds-usage-admin
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        data: |
          postgresql://{{ .username }}:{{ .password }}@{{ .endpoint }}/{{ .db_name }}?sslmode=require
        username: "{{ .username }}"
        password: "{{ .password }}"
        endpoint: "{{ .endpoint }}"
        db_name: "{{ .db_name }}"
  data:
    - { secretKey: username, remoteRef: { key: saas-usage-db-secret, property: username } }
    - { secretKey: password, remoteRef: { key: saas-usage-db-secret, property: password } }
    - { secretKey: endpoint, remoteRef: { key: saas-usage-db-secret, property: endpoint } }
    - { secretKey: db_name,  remoteRef: { key: saas-usage-db-secret, property: db_name  } }
---
apiVersion: postgresql.sql.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: rds-usage
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  credentials:
    source: PostgreSQLConnectionSecret
    connectionSecretRef:
      namespace: crossplane-system
      name: rds-usage-admin
  sslMode: require
