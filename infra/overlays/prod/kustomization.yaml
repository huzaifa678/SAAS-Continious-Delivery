---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Helm addons moved to the flattened `addons` ApplicationSet. Prod delivers only
# the crossplane provider-sql wiring (contract-generated).
resources:
  - crossplane/provider-sql.yaml
