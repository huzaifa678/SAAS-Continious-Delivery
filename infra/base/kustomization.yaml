---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# airflow, argocd-image-updater, keda, argo-rollouts and crossplane moved to the
# flattened `addons` ApplicationSet (gitops/infra/*). What remains here is raw
# platform manifests delivered directly by the `infra` ApplicationSet.
resources:
  - pod-identity-refresh/application.yaml
