---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - airflow/airflow.yaml
  - airflow/appdatabase.yaml
  # keycloak moved to the `addons` ApplicationSet (gitops/infra/keycloak).
  - pod-identity-refresh/application.yaml
