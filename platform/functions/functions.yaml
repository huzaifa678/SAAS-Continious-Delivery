# ─────────────────────────────────────────────────────────────────────────────
# GENERATED by scripts/render_gitops.py from the Terraform gitops_contract,
# using the templates under scripts/templates/*.tpl.
# Do not edit by hand — change the contract in saas-services-infra (or the
# template) and re-render.
# ─────────────────────────────────────────────────────────────────────────────
---
# see .github/workflows/build-function.yml.
apiVersion: pkg.crossplane.io/v1
kind: Function
metadata:
  name: function-appdatabase
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  package: 000000000000.dkr.ecr.us-east-1.amazonaws.com/function-appdatabase:sha-0000000
  packagePullPolicy: IfNotPresent
  revisionActivationPolicy: Automatic
  revisionHistoryLimit: 1
---
# Custom function composing the ElastiCache resources for XAppCache
# (source: functions/function-appcache), branching on the claim's mode.
apiVersion: pkg.crossplane.io/v1
kind: Function
metadata:
  name: function-appcache
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  package: 000000000000.dkr.ecr.us-east-1.amazonaws.com/function-appcache:sha-0000000
  packagePullPolicy: IfNotPresent
  revisionActivationPolicy: Automatic
  revisionHistoryLimit: 1
---
apiVersion: pkg.crossplane.io/v1
kind: Function
metadata:
  name: function-auto-ready
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  package: xpkg.upbound.io/crossplane-contrib/function-auto-ready:v0.4.2
  packagePullPolicy: IfNotPresent
  revisionActivationPolicy: Automatic
  revisionHistoryLimit: 1
