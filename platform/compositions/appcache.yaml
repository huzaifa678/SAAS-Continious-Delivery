# ─────────────────────────────────────────────────────────────────────────────
# GENERATED by scripts/gen/appcache-composition (go run ./scripts/gen/...).
# Modern Crossplane function-pipeline Composition — do not edit by hand.
# Change the generator and re-run 'make composition'.
# ─────────────────────────────────────────────────────────────────────────────
---
apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  labels:
    provider: aws
    type: appcache
  name: appcache.platform.saas.example
spec:
  compositeTypeRef:
    apiVersion: platform.saas.example/v1alpha1
    kind: XAppCache
  mode: Pipeline
  pipeline:
  - functionRef:
      name: function-appcache
    input:
      apiVersion: appcache.fn.saas.example/v1beta1
      defaultUserGroupId: saas-redis
      kind: Input
      providerConfigName: aws-elasticache
    step: appcache
  - functionRef:
      name: function-auto-ready
    step: ready
  writeConnectionSecretsToNamespace: crossplane-system
