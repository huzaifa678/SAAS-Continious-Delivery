# ─────────────────────────────────────────────────────────────────────────────
# GENERATED by scripts/gen/appdatabase-composition (go run ./scripts/gen/...).
# Modern Crossplane function-pipeline Composition — do not edit by hand.
# Change the generator and re-run 'make composition'.
# ─────────────────────────────────────────────────────────────────────────────
---
apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  labels:
    provider: postgresql
    type: appdatabase
  name: appdatabase.postgres.platform.saas.example
spec:
  compositeTypeRef:
    apiVersion: platform.saas.example/v1alpha1
    kind: XAppDatabase
  mode: Pipeline
  pipeline:
  - functionRef:
      name: function-appdatabase
    input:
      apiVersion: appdatabase.fn.saas.example/v1beta1
      connectionSecretNamespace: crossplane-system
      kind: Input
      providerConfigFormat: rds-%s
    step: appdatabase
  - functionRef:
      name: function-auto-ready
    step: ready
  writeConnectionSecretsToNamespace: crossplane-system
